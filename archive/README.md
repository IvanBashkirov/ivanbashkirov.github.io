# Content archive

Snapshots of `content/` removed from the live site.

- `seed-content-2026-07-16/` — the full launch seed: 47 log entries, 6 project
  channels, 14 docs (including the four ported pre-2021 essays and the hidden
  `untitled-jig` tune). To restore anything, copy files back into `content/`
  (docs into `content/docs/`, entries back into `content/log.yaml` keeping
  `n` sequential and dates chronological).
